<div class="page-header">
  <h1>
    My Events
    <a href="/events/new" class="btn btn-lg btn-success">New Event</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th></th>
        <th>Title</th>
        <th>Location</th>
        <th>Date</th>
      </tr>

      <% @events.each do |event| %>
      <tr>
        <td><img  src="<%=event.receipt%>"  height= "150px"></td>
        <td><%= event.title %></td>
          <td><%= event.location %></td>
              <td><%= event.date %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
